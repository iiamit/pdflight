# Interpretation verification notes

Written by `tools/verify_interps.py`, with discovery outcomes folded in from
`tools/discover_interps.py`.

Records a checked result for all thirty-four candidates, passes included, plus
what was ruled out for anything that failed. An entry that cannot be confirmed
is dropped rather than guessed at, and never replaced by a plausible-looking
substitute.

Sixteen of the thirty-four were unverified at handoff. The deferred list holds
only seven, so if more than seven fail the set cannot be held at thirty-four.
The real number is reported here rather than padded.

## Results

Not yet generated. Phase 1 deliverable 1.4.
